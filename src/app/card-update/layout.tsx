export default function CardUpdateLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ minHeight: '100vh', background: '#f9fafb', color: '#111827' }}>
      {children}
    </div>
  )
}
