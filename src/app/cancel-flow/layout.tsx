export default function CancelFlowLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ minHeight: '100vh', background: '#fff', color: '#111827' }}>
      {children}
    </div>
  )
}
